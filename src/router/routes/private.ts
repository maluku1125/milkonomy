import type { RouteRecordRaw } from "vue-router"

/**
 * 私有路由配置（個人版精簡：已清空）
 * 全部進階頁面（超級強化、打野系列、繼承、分解、撿漏、製作煉金、超級強化分解、Demo）已移除。
 * 保留空陣列以避免 router/index.ts 引用時報錯。
 */
export const privateRoutes: RouteRecordRaw[] = []
